#include <stdio.h>

#include "arthematics.h"

void app_main(void)
{
    printf("The add is %d \n", add(10, 5));
    printf("The sub is %d \n", sub(10, 5));
    printf("The mul is %d \n", mul(10, 5));
}
